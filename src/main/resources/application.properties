spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASS}

# AVAILABLE PROFILES:
# [repositories] spring-data-jpa,hibernate-jpa
# [data set sources] european-central-bank
# Note: Pick one of each
spring.profiles.active=spring-data-jpa,european-central-bank

spring.jpa.generate-ddl=true
spring.jpa.show-sql=false
